# Copy to inventory.ini and edit.
#
# The role needs nothing from the inventory beyond reachable hosts and a way to
# become root.

[mailservers]
mx1.example.com
mx2.example.com

[mailservers:vars]
ansible_user=ansible
ansible_become=true
